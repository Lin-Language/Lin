// Edit Distance — minimum insertions, deletions, or substitutions to transform a into b.
// TODO: fill a 2-D DP table, dp[i][j] = edit distance for a[0..i) and b[0..j).

export val solve = (a: String, b: String): Int32 =>
  0
