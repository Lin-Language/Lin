[workspace]
# Act only through the release PR; merging it cuts the release.
release_always = false
# Nothing is published to crates.io (all deps are path-only).
publish = false
# Use git tags (not crates.io) as the version baseline. Required because the
# crates are never published, so the registry has nothing to compare against;
# without this release-plz finds no baseline and opens no PR. With no vX.Y.Z tag
# yet, this makes it treat the next run as the initial 1.0.0 release.
git_only = true
# Create the GitHub release and git tag.
git_release_enable = true
git_tag_enable = true
git_tag_name = "v{{ version }}"

[changelog]
header = "# Changelog\n\nAll notable changes to Lin are documented here.\n"
body = """
## [{{ version }}]{%- if release_link %}({{ release_link }}){% endif %} - {{ timestamp | date(format="%Y-%m-%d") }}
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}
- {% if commit.scope %}**{{ commit.scope }}**: {% endif %}{{ commit.message | upper_first }}
{%- endfor %}
{% endfor %}
"""
commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^perf", group = "Performance" },
  { message = "^docs", group = "Documentation" },
  { message = "^refactor", group = "Refactor" },
  { body = ".*BREAKING CHANGE.*", group = "Breaking Changes" },
  { message = "^.*!:", group = "Breaking Changes" },
  { message = "^chore", skip = true },
  { message = "^ci", skip = true },
  { message = "^test", skip = true },
  { message = "^style", skip = true },
  { message = ".*", group = "Other" },
]
