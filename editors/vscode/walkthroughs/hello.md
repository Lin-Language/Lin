# Write your first Lin program

Create a file named `hello.lin` and paste:

```lin
import { print } from "std/io"

print("Hello, Lin!")
```

As you type, the bundled language server gives you inline diagnostics, hover
types, inlay hints, and dot-completion with auto-import.
